(1) Создаем новый Регистр накопления с названием СтоимостьМатериалов

![[Pasted image 20251209164007.png]]

(2) Зададим подсистемы

![[Pasted image 20251209164015.png]]

(3) Зададим Данные

![[Pasted image 20251209164331.png]]

![[Pasted image 20251209164349.png]]

(4) Добавляем в панель навигации наш регистр Стоимость материалов

Добавляем в подсистемы Бухгалтерия, УчетМатериалов, ОказаниеУслуг

![[Pasted image 20251209165107.png]]

(5) Добавим в Движения Документа ПриходнаяНакладная наш новый регистр СтоимостьМатериалов

![[Pasted image 20251209165702.png]]

(6) Изменение процедуры проведения

Переходим в Конструктор движений

![[Pasted image 20251209165956.png]]

![[Pasted image 20251209170425.png]]

![[Pasted image 20251209170657.png]]

После чего нажимаем ОК

Исправим код следующим образом:

```1c
Процедура ОбработкаПроведения(Отказ, Режим)

	Движения.ОстаткиМатериалов.Записывать = Истина;
	Движения.СтоимостьМатериалов.Записывать = Истина;
	
	Для Каждого ТекСтрокаМатериалы Из Материалы Цикл
		Движение = Движения.ОстаткиМатериалов.Добавить();
		Движение.ВидДвижения = ВидДвиженияНакопления.Приход;
		Движение.Период = Дата;
		Движение.Материал = ТекСтрокаМатериалы.Материал;
		Движение.Склад = Склад;
		Движение.Количество = ТекСтрокаМатериалы.Количество;
		
		// Регистр Стоимость Материалов Приход
		Движение = Движения.СтоимостьМатериалов.Добавить();
		Движение.ВидДвижения = ВидДвиженияНакопления.Приход;
		Движение.Период = Дата;
		Движение.Материал = ТекСтрокаМатериалы.Материал;
		Движение.Стоимость = ТекСтрокаМатериалы.Сумма;
	КонецЦикла;

КонецПроцедуры
```

(7) Переходим на форму нашего Документа ПриходнаяНакладная

![[Pasted image 20251209171653.png]]

(8) Создаем новый реквизит Стоимость в Документе ОказаниеУслуги 

![[Pasted image 20251209172441.png]]

(9) Откроем форму Документа ОказаниеУслуги и перетащим Стоимость из правого окна в левый список. Поставим поле Стоимость после поля Номенклатура

![[Pasted image 20251209172845.png]]

(10) В Движениях добавим регистр СтоимостьМатериалов

![[Pasted image 20251209173424.png]]

(11) В модуле объекта Документа ОказаниеУслуги. Изменим код следующим образом:

```1c
Процедура ОбработкаПроведения(Отказ, Режим)

	Движения.ОстаткиМатериалов.Записывать = Истина;
	Движения.СтоимостьМатериалов.Записывать = Истина;
	
	Для Каждого ТекСтрокаПереченьНоменклатуры Из ПереченьНоменклатуры Цикл
		Если ТекСтрокаПереченьНоменклатуры.Номенклатура.ВидНоменклатуры = Перечисления.ВидыНоменклатуры.Материал Тогда
			Движение = Движения.ОстаткиМатериалов.Добавить();
			Движение.ВидДвижения = ВидДвиженияНакопления.Расход;
			Движение.Период = Дата;
			Движение.Материал = ТекСтрокаПереченьНоменклатуры.Номенклатура;
			Движение.Склад = Склад;
			Движение.Количество = ТекСтрокаПереченьНоменклатуры.Количество;
			
			// регистр СтоимостьМатериалов Расход
			Движение = Движения.СтоимостьМатериалов.Добавить();
			Движение.ВидДвижения = ВидДвиженияНакопления.Расход;
			Движение.Период = Дата;
			Движение.Материал = ТекСтрокаПереченьНоменклатуры.Номенклатура;
			Движение.Стоимость = ТекСтрокаПереченьНоменклатуры.Количество * ТекСтрокаПереченьНоменклатуры.Стоимость;
			
		КонецЕсли;
	КонецЦикла;

КонецПроцедуры
```


